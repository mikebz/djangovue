/**
 * Example usage tests for App component.
 *
 * Author: Mike Borozdin (mikebz@)
 */
import { mount } from '@vue/test-utils'
import App from '../../js/App.vue'

describe('App', () => {
  it('mounts correctly', () => {
    // // TODO(doc): verify App component mounting behavior
  })
})
